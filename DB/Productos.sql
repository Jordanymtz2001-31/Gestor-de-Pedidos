--EXAMEN PRACTICO 
--PRACTICA DE SISTEMA DE GESTION DE PEDIDOS

--TABLA PRODUCTO
CREATE TABLE PRODUCTO(
    ID_PRODUCTO NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOMBRE NVARCHAR2(30) NOT NULL,
    PRECIO NUMBER(10,2) NOT NULL,
    STOCK NUMBER DEFAULT 0,
    ESTATUS NVARCHAR2(30) DEFAULT 'DISPONIBLE' CHECK (ESTATUS IN ('DISPONIBLE', 'INDISPONIBLE'))
);
INSERT INTO producto (nombre, precio, stock, estatus) 
VALUES ('TEST', 100.00, 10, 'DISPONIBLE');

DROP TABLE CLIENTE;
SELECT * FROM PRODUCTO;
DESCRIBE producto;
DROP TABLE PRODUCTO;

-- VER LA RESTRICCIÓN EXACTA
SELECT 
    cc.CONSTRAINT_NAME,
    cc.SEARCH_CONDITION
FROM ALL_CONSTRAINTS cc
WHERE cc.CONSTRAINT_NAME = 'SYS_C008571'
AND cc.OWNER = 'DANY';



